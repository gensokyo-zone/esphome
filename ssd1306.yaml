substitutions:
  display_lambda: ""
  display_rotation: "0"

font:
  - file: ${fonts_root}/cozette.bdf
    id: cozette

display:
  - platform: ssd1306_i2c
    model: SSD1306 128x64
    id: ssd1306_display
    address: 0x3C
    contrast: 50%
    rotation: ${display_rotation}
    lambda: |-
      ${display_lambda}
